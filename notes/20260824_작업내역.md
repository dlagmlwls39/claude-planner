# 20260824 작업내역

## 개요
Supabase 연동 트러블슈팅 + 하늘색 기반 디자인 전면 개편.

## 1. Supabase 연결 문제 해결
- `.env.local`의 `NEXT_PUBLIC_SUPABASE_URL` 끝에 `/rest/v1/`가 붙어 있어 회원가입 시
  `Invalid path specified in request URL` 발생 → `https://<project>.supabase.co`까지만 남기도록 수정.
- `no session` 에러: Supabase 이메일 인증(Confirm email)이 켜져 있으면 `signUp` 직후 세션이 없어
  `ensureProfile()`(lib/supabase/friends.ts:12)에서 throw. → 대시보드에서 개발 중 Confirm email 끄도록 안내.

## 2. 디자인 개편 (핑크 파스텔 → 하늘색 기반)
### 디자인 시스템 (app/globals.css)
- `@theme` 팔레트 교체: `cream`(배경) / `brand`(스카이블루) / `brand-dark` / `brand-soft` /
  `mint` / `lavender` / `peach` / `ink`(블루그레이 텍스트) / `line`(테두리).
- 공통 컴포넌트 클래스 신설: `.card`, `.field`, `.btn`(+`btn-primary`/`btn-soft`/`btn-mint`/`btn-ghost`).
- 전역 커서 규칙 추가: 버튼·링크·체크박스 라벨은 pointer, 비활성 버튼은 not-allowed.

### 레이아웃 폭 통일
- 문제: 페이지마다 컨테이너 여백이 제각각(`px-5`/`px-3`/`p-4`)이라 탭 전환 시 가로폭이 튐.
- 해결: 모든 메인 페이지 루트를 `px-4 pt-6`으로 통일. MonthGrid는 자체 `px-3` 제거하고 `.card`로 감쌈.

## 수정한 파일
- `app/globals.css` — 팔레트/공통 클래스/커서 (전면 재작성)
- `app/layout.tsx` — body 색상 토큰, viewport themeColor 하늘색
- `app/login/page.tsx` — 카드형 로그인 UI, 로고 헤더
- `app/(main)/layout.tsx` — bg-cream, 하단 여백 pb-24
- `app/(main)/calendar/page.tsx`, `app/(main)/friends/[id]/page.tsx` — px-4 통일, 헤더 화살표 버튼 스타일
- `app/(main)/todos/page.tsx`, `friends/page.tsx`, `profile/page.tsx` — px-4 통일, 헤더/카드 리스타일
- `components/ui/BottomTabBar.tsx` — 라운드 상단·그림자·활성 pill, safe-area 패딩
- `components/ui/BottomSheet.tsx` — 핸들바, 그림자
- `components/calendar/MonthGrid.tsx` — 카드 래핑, 주말 색상
- `components/calendar/DayCell.tsx` — 선택/오늘 상태 하늘색
- `components/calendar/DaySheet.tsx`, `EventForm.tsx` — .field/.btn 적용, 색상칩 강조
- `components/todos/TodoItem.tsx`, `friends/FriendSearch.tsx`, `friends/FriendList.tsx` — 카드·버튼 리스타일
- `public/manifest.webmanifest` — background/theme_color 하늘색

## 3. 이메일 인증 UX + 브랜딩
- 회원가입 시 `signUp` 응답에 세션이 없으면(이메일 인증 ON) `no session` throw 대신
  "📮 이메일 인증을 진행해 주세요" 모달 표시 (app/login/page.tsx).
- 인증 대기 상태에선 프로필 생성 불가 → 닉네임을 `localStorage.pending_nickname` 에 저장,
  인증 후 첫 로그인 시 `ensureProfile`로 생성(없으면 이메일 앞부분 사용).
- 파비콘 교체: Next 기본 `app/favicon.ico` 삭제, `app/icon.svg`(하늘색 캘린더) 추가.
  `public/icon.svg` 도 추가하고 manifest icons에 등록.
- 인증 메일 템플릿 신규: `supabase/email-templates/confirm-signup.html`
  (대시보드 Authentication → Emails → Templates → Confirm signup 에 붙여넣기용, 하늘색 컨셉).

## 4. 추가 개선 (에러 한글화 / 폭 통일 / 아이콘 / 폰트)
- 에러 한글화: `lib/auth-errors.ts` 신규. Supabase auth 에러를 code/message로 매칭해
  한국어 문구로 변환. 로그인 페이지 catch에서 `translateAuthError` 사용.
- 컨텐츠 폭 통일: `app/(main)/layout.tsx` 컨테이너에 `w-full` 추가.
  (원인: flex-col 자식에 `mx-auto`가 있으면 stretch가 꺼져 컨테이너가 내용
  크기로 줄어들어 페이지마다 폭이 달라짐. w-full로 480px 고정 → 탭바와 일치.)
- 아이콘 통일: `components/ui/icons.tsx` 신규(Calendar/Friends/Check/User 라인 아이콘).
  BottomTabBar의 이모지를 SVG 아이콘으로 교체. 페이지 제목/로그인 로고의
  이모지 제거·교체. app/public icon.svg 를 톤 통일된 깔끔한 캘린더로 리디자인.
- 폰트(최초): 아리따 돋움 Medium woff self-host → **이후 나눔스퀘어로 교체**(아래 5번).

## 5. 폰트 나눔스퀘어 네오 교체 + 타이틀 굵게
- 아리따 돋움 → 나눔스퀘어 → 최종 **네이버 나눔스퀘어 네오(NanumSquareNeo)** 로 교체.
  - `public/fonts/NanumSquareNeo-Variable.woff2` **가변폰트 1개** self-host
    (weight 300~900). 출처: moonspam/NanumSquareNeo (jsDelivr).
  - globals.css `@font-face`(font-weight 300 900) + `--font-kr`/`--font-sans`/body 지정.
  - 기존 나눔스퀘어 정적 woff2 4종은 제거.
  - 기존 `Arita-dotum-Medium.woff` 및 `--font-arita` 제거, 아리따 라이선스
    고지 문구도 삭제(내 정보 화면).
- 타이틀 굵게 처리: 각 화면 제목 h1 및 캘린더 년/월 버튼을 `font-extrabold`(800,
  NanumSquareEB)로 변경 — todos/friends/profile/login/친구캘린더/캘린더 헤더.

> 비고: 캘린더 상단에 년/월 선택 드롭다운(picker)이 추가됨(app/(main)/calendar/page.tsx,
> lib/date.ts getYearOptions). 사용자 편집 반영분.

## 6. 타이틀 굵기 원복 + 프로필 수정(닉네임/사진)
- 타이틀 `font-extrabold` → `font-bold` 로 원복(요청).
- 프로필 수정 기능 추가:
  - 내 정보에 닉네임 표시(기존 이메일 앞부분처럼 보이던 값 수정 가능) + [수정] 버튼.
  - 수정 모드에서 닉네임 변경 및 프로필 **사진 업로드** 가능.
  - `lib/supabase/profile.ts` 신규: `uploadAvatar`(5MB/이미지 제한, avatars 버킷),
    `updateMyProfile`(닉네임 중복 시 한국어 에러).
  - `components/ui/Avatar.tsx` 신규: 사진 있으면 이미지, 없으면 색상 원+이니셜.
    프로필/친구목록/친구검색에 공통 적용.
  - 타입 `Profile.avatar_url` 추가.
- DB/스토리지: `supabase/migrations/0002_avatar_upload.sql` 신규
  (profiles.avatar_url 컬럼, avatars 공개 버킷, 소유자 기준 storage 정책).
  **대시보드 SQL Editor에서 실행 필요.**

## 7. 프로필 사진 삭제 / 비밀번호 변경 / 공개 아이콘 변경
- 프로필 수정에 **사진 삭제** 추가: avatar_url=null 로 갱신해 기본(색상) 아바타로 복귀.
  (updateMyProfile avatar_url 타입 `string|null` 허용)
- **비밀번호 변경** 추가: 내 정보에 접이식 카드. `updatePassword`(supabase.auth.updateUser,
  6자 이상 검증, 확인란 일치 검사). profile.ts에 함수 추가.
- 일정 "친구 공개" 표시 아이콘을 🌷 → **FriendsIcon(두 사람)** 으로 교체(DaySheet).

## 8. 친구와 함께 일정 등록 (공유 일정 + 초대)
- 설계: `docs/superpowers/specs/2026-08-24-shared-event-invite-design.md`
- 모델: 원본 1개 + `event_participants`(pending/accepted/declined). 각자 사본 아님 →
  주최자 수정/삭제가 참여자 전원에게 동시 반영. 초대→수락(동의 기반).
- 마이그레이션 `supabase/migrations/0003_event_participants.sql`
  (테이블, 헬퍼 is_event_participant/is_event_owner, events 참여자 select 정책,
  event_participants RLS). **대시보드에서 실행 필요.**
- `lib/supabase/event-invites.ts` 신규: invite/list/accept/decline.
- `lib/supabase/events.ts`의 listMyEventsInRange를 "내 일정 + 수락한 공유 일정"으로 확장
  (내 일정은 user_id 명시 필터로 친구 공개일정 혼입 방지).
- UI: EventForm에 "친구와 함께하기" 다중선택(신규 일정만), DaySheet 저장 시 초대 발송,
  친구 페이지에 EventInviteList("받은 일정 초대") 추가.
- 권한: 수정/삭제는 주최자만(기존 events RLS). 참여자는 나가기(참여 레코드 삭제) 가능.

## 검증
- `npm run build` 성공 (타입 체크 통과, /icon.svg 라우트 생성 확인).
- pastel-*/이모지/Arita 잔재 grep 결과 없음.
- 공유 일정: 두 계정 초대→수락→양쪽 표시→주최자 삭제 동시 반영은 0003 실행 후 수동 확인 필요.

## 주의사항 / 남은 것
- `.env.local` 수정 후에는 dev 서버 재시작 필요(Next.js는 시작 시 1회만 로드).
- 실배포 시 Confirm email 다시 켜면 "메일 확인" 안내 화면이 없음 → 필요 시 추가.
- `public/icon-192.png`, `icon-512.png` 는 아직 실제 디자인 아이콘으로 교체 필요.
